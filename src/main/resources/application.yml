spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/lab03?useUnicode=true&characterEncoding=utf8&serverTimezone=UTC&allowMultiQueries=true

mybatis-plus:
  mapper-locations: classpath:mapper/*Mapper.xml
  type-aliases-package: com.example.lab03.enetity

server:
  port: 8081

